export interface LoginParams {
    name: string | null;
    password: string | null;
}

export interface RegisterParams {
    name: string | null,
    email: string | null,
    userIcon: string | null,
    password: string | null,
}
